import { useWallet } from "@/contexts/WalletContext";
import { TokenLogo } from "./tokenLogo";

export const WalletInfo = () => {
  // This component is no longer needed since we unified everything into IvaStatus
  return null;
}; 